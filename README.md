# memory_agent

Persistent memory for AI agents (Cowork, Cursor, Claude Code).

- `lightrag-legacy/` — archive of the previous LightRAG/BrainAI setup (MCP server, tray app, memory skill, docs). Knowledge-graph data and `.env` are kept locally only.
